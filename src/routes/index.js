import { Switch } from "react-router-dom";
import { Route } from "./route";

export const Routes = () => {
  return (
    <Switch>
      {/* <Route exact path=''/>
            <Route/>
            <Route/> */}
    </Switch>
  );
};
